<%= render partial: "all_task_options", locals:{overdue: @overdue, active: @active, complete: @complete} %>

<div class="task-block">
  <h2 class="t">All Overdue Tasks</h2>
  <div class="line-dark"></div>
  <% if @tasks.empty? %>
    <h4 class="center">It looks like you don't have overdue tasks. Good job!</h4>
  <% else %>
    <div class="row">
      <% @tasks.each_with_index do |task, index| %>
        <%= render partial: "task", locals: {task: task, index: index } %>
      <%end %>
    </div>
  <% end %>
</div>




